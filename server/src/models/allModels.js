export const Disciplinas = {
    _id,
    nombre,
    color,
    estado
}

export const Tar_contenidos = {
    _id,
    id_tarjeta,
    contenido,
    estado
}

export const Tar_ejemplos = {
    _id,
    id_tarjeta,
    ejemplo,
    estado
}

export const Tarjetas = {
    _id,
    id_tema_estudio,
    titulo,
    color,
    estado
}

export const TemasEstudio = {
    id,
    id_disciplina,
    nombre,
    color,
    tag,
    estado
}
